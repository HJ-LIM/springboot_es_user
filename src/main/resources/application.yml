server.port: 9090

spring.mvc.view.prefix: /WEB-INF/layout/
spring.mvc.view.suffix: .jsp

restTemplate:
  factory:
    readTimeout: 5000
    connectTimeout: 3000
  httpClient:
    maxConnTotal: 100
    maxConnPerRoute: 5

elasticsearch:
  remote:
    cluster:
      name: drms
    server: 10.10.0.242
    port: 19300

logging:
  level:
    root: warn
    com:
      simple:
        juni:
          elasticsearch: info
